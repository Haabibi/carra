window.CarraItemInsights = (() => {
  const esc = value => String(value).replace(/[&<>"']/g, c => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[c]));
  const money = value => CarraCore.money(value);
  function illustration(kind) {
    const photo = window.CarraPartPhotos?.[kind];
    if (photo) return `<figure class="part-visual part-photo"><a href="${esc(photo.src)}" target="_blank" rel="noopener noreferrer" aria-label="Enlarge reference photo: ${esc(photo.alt)}"><img src="${esc(photo.src)}" alt="${esc(photo.alt)}" width="640" height="480" loading="lazy" decoding="async" onerror="this.hidden=true;this.nextElementSibling.hidden=false"><span data-photo-fallback hidden>Reference photo unavailable</span></a><figcaption>${kind === "charging" ? "Related part: 12V battery" : "Reference photo"} · not your vehicle<details><summary>Photo credit</summary><span>${esc(photo.author)}</span><a href="${esc(photo.source)}" target="_blank" rel="noopener noreferrer">Wikimedia Commons source</a>${photo.licenseUrl ? `<a href="${esc(photo.licenseUrl)}" target="_blank" rel="noopener noreferrer">${esc(photo.license)}</a>` : `<span>${esc(photo.license)}</span>`}</details></figcaption></figure>`;
    const brake = ['pads', 'rotors', 'fluid'].includes(kind);
    const battery = ['battery', 'charging'].includes(kind);
    if (!brake && !battery) return '';
    const drawing = brake ? `<circle cx="142" cy="95" r="64" fill="#dce7ed" stroke="#6d8797" stroke-width="4"/><circle cx="142" cy="95" r="22" fill="#f8fbfd" stroke="#6d8797" stroke-width="3"/><path d="M89 51 Q59 95 89 139" fill="none" stroke="#1d7596" stroke-width="19" stroke-linecap="round"/><path d="M195 51 Q225 95 195 139" fill="none" stroke="#1d7596" stroke-width="19" stroke-linecap="round"/><path d="M63 84H26M206 64l25-33h53M172 135l33 36h69" fill="none" stroke="#486471" stroke-width="2"/><text x="12" y="72">Pads</text><text x="238" y="24">Pads</text><text x="224" y="189">Rotor</text>` : `<rect x="79" y="54" width="145" height="104" rx="12" fill="#dce7ed" stroke="#6d8797" stroke-width="3"/><path d="M98 54V39h24v15m59 0V39h24v15" fill="#6d8797"/><path d="M103 88h24m-12-12v24m49-12h25" stroke="#1d7596" stroke-width="5"/><text x="112" y="137" font-size="22">12 V</text><path d="M37 109H72m158 0h40" stroke="#1d7596" stroke-width="3"/><text x="66" y="190">Low-voltage battery</text>`;
    return `<figure class="part-visual"><svg viewBox="0 0 310 210" role="img" aria-label="${brake ? 'Simplified diagram: brake pads press against the rotor' : 'Simplified diagram of a 12-volt battery'}" xmlns="http://www.w3.org/2000/svg">${drawing}</svg><figcaption>Illustration · not your vehicle</figcaption></figure>`;
  }
  function render(item, vehicle) {
    const info = CarraCore.itemInsight(item, vehicle);
    const c = info.comparison;
    const price = c ? `<section class="price-context" aria-label="Price reference"><div class="price-context-head"><h4>${c.position} the reference range</h4><span>Published reference</span></div><p>${esc(c.scope)}: <strong>${money(c.low)}–${money(c.high)}</strong></p><p class="price-scope">Parts + labor · ZIP code, model year, and axle not matched</p><div class="price-scale" role="img" aria-label="Listed amount ${money(item.total)}, ${c.position.toLowerCase()} the reference range ${money(c.low)} to ${money(c.high)}"><div class="price-track" style="--band-start:${c.bandStart}%;--band-end:${c.bandEnd}%"><span class="price-marker" style="left:${c.marker}%"></span></div><div class="price-labels"><span>Lower</span><span>Reference range</span><span>Higher</span></div></div><p class="price-takeaway">${c.difference ? `${money(c.difference)} ${c.position === 'Above' ? 'above the upper' : 'below the lower'} reference. ` : 'The listed amount falls inside this published range. '}Confirm the same parts and labor are included.</p><details><summary>Source & comparison limits</summary><p>Parts and labor; taxes and fees excluded. Year, ZIP code, front/rear axle, parts brand, and combined-job labor have not been matched. This is context, not a local quote or a verdict on the shop.</p><a href="${c.url}" target="_blank" rel="noopener noreferrer">RepairPal cost reference</a><small>Retrieved Sep 19, 2026 · Static snapshot</small></details></section>` : `<div class="price-unavailable"><strong>No comparable price range yet</strong><p>${item.total == null ? 'The line amount is missing.' : 'We do not have a matching reference for this service.'} Ask for the exact scope and parts/labor breakdown.</p></div>`;
    return `<div class="part-explainer">${illustration(info.kind)}<div><h4>What it does</h4><p>${esc(info.description)}</p>${info.source ? `<a class="part-source" href="${info.source}" target="_blank" rel="noopener noreferrer">Learn about this part</a>` : ''}</div></div>${price}<div class="item-next-question"><span>One useful question</span><p>${esc(info.question)}</p></div>`;
  }
  return { render };
})();
