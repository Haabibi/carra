# Copy this file to .env and add your Google AI Studio key locally.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-3.6-flash

# PDF/photo reading uses this server-side key. Keep .env out of source control.
# The provided dataset samples work without a key; arbitrary uploads require it.
